cmake_minimum_required(VERSION 3.16)
project(qingjian-fcitx5 VERSION 0.1.0 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
option(QINGJIAN_RENDER_FFI "Link the Rust render-ffi static library" OFF)
option(QINGJIAN_GNOME_PROBE "Build the isolated GNOME stage-0 candidate experiment" OFF)
# 旧缓存仅迁移一次；显式的新开关优先，避免旧 OFF 覆盖新构建。
if(DEFINED QINGJIAN_EXPERIMENTAL_X11 AND NOT DEFINED QINGJIAN_X11_BACKEND)
  set(QINGJIAN_X11_BACKEND "${QINGJIAN_EXPERIMENTAL_X11}" CACHE BOOL "Build the optional X11 candidate surface backend")
endif()
unset(QINGJIAN_EXPERIMENTAL_X11 CACHE)
option(QINGJIAN_X11_BACKEND "Build the optional X11 candidate surface backend" ON)
find_package(PkgConfig REQUIRED)
find_package(nlohmann_json REQUIRED)
find_package(Threads REQUIRED)
pkg_check_modules(FCITX5 REQUIRED IMPORTED_TARGET Fcitx5Core>=5.1.9)
set(QINGJIAN_XCB_SOURCES src/panel/backend/xcb.cpp src/panel/backend/screens.cpp src/panel/backend/upload.cpp)
set(QINGJIAN_PANEL_SOURCES src/panel/controller.cpp src/panel/backend/probe.cpp src/panel/backend/selector.cpp ${QINGJIAN_XCB_SOURCES} src/panel/appearance.cpp)
if(QINGJIAN_GNOME_PROBE)
  if(NOT QINGJIAN_RENDER_FFI)
    message(FATAL_ERROR "The GNOME probe needs QINGJIAN_RENDER_FFI")
  endif()
  list(APPEND QINGJIAN_PANEL_SOURCES src/panel/gnome/probe.cpp)
endif()
set(QINGJIAN_SOURCES src/qingjian.cpp src/factory.cpp src/ipc/connection.cpp src/key/mapping.cpp ${QINGJIAN_PANEL_SOURCES})
if(QINGJIAN_RENDER_FFI)
  set(QINGJIAN_RENDER_FFI_LIB "" CACHE FILEPATH "Path to libqingjian_render_ffi.a")
  set(QINGJIAN_RENDER_FFI_INCLUDE "${CMAKE_CURRENT_SOURCE_DIR}/../render-ffi/include" CACHE PATH "Directory containing qingjian_render.h")
  if(NOT EXISTS "${QINGJIAN_RENDER_FFI_LIB}" OR NOT EXISTS "${QINGJIAN_RENDER_FFI_INCLUDE}/qingjian_render.h")
    message(FATAL_ERROR "Build qingjian-render-ffi first and set QINGJIAN_RENDER_FFI_LIB")
  endif()
endif()
if(QINGJIAN_X11_BACKEND)
  pkg_check_modules(XCB REQUIRED IMPORTED_TARGET xcb xcb-render xcb-randr)
endif()
function(qingjian_target target)
  if(QINGJIAN_GNOME_PROBE)
    target_compile_definitions(${target} PRIVATE QJ_GNOME_PROBE=1)
  endif()
  target_include_directories(${target} PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  target_compile_options(${target} PRIVATE -Wall -Wextra -Werror)
  target_link_libraries(${target} PRIVATE PkgConfig::FCITX5 nlohmann_json::nlohmann_json Threads::Threads)
  if(QINGJIAN_RENDER_FFI)
    target_compile_definitions(${target} PRIVATE QJ_RENDER_FFI=1)
    target_include_directories(${target} PRIVATE "${QINGJIAN_RENDER_FFI_INCLUDE}")
    target_link_libraries(${target} PRIVATE "${QINGJIAN_RENDER_FFI_LIB}" ${CMAKE_DL_LIBS} m)
    # 静态 archive 的未用业务/网络对象不进入插件；Rust 内部符号不污染 Fcitx。
    target_link_options(${target} PRIVATE "LINKER:--gc-sections" "LINKER:--exclude-libs,ALL")
  endif()
  if(QINGJIAN_X11_BACKEND)
    target_compile_definitions(${target} PRIVATE QJ_X11_BACKEND=1)
    target_link_libraries(${target} PRIVATE PkgConfig::XCB)
  endif()
endfunction()
add_library(qingjian MODULE ${QINGJIAN_SOURCES})
qingjian_target(qingjian)
set_target_properties(qingjian PROPERTIES PREFIX "" OUTPUT_NAME "qingjian")
install(TARGETS qingjian LIBRARY DESTINATION lib/fcitx5)
install(FILES data/addon/qingjian.conf DESTINATION share/fcitx5/addon)
install(FILES data/inputmethod/qingjian.conf DESTINATION share/fcitx5/inputmethod)
install(FILES ../../../assets/icon/logo.png DESTINATION share/pixmaps RENAME qingjian.png)

include(CTest)
if(BUILD_TESTING)
  add_executable(qingjian-size tests/size.cpp)
  target_include_directories(qingjian-size PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  target_link_libraries(qingjian-size PRIVATE nlohmann_json::nlohmann_json)
  target_compile_options(qingjian-size PRIVATE -Wall -Wextra -Werror -UNDEBUG)
  add_test(NAME qingjian-size COMMAND qingjian-size)
  add_executable(qingjian-upload tests/upload.cpp src/panel/backend/upload.cpp)
  target_include_directories(qingjian-upload PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  target_compile_options(qingjian-upload PRIVATE -Wall -Wextra -Werror -UNDEBUG)
  add_test(NAME qingjian-upload COMMAND qingjian-upload)
  add_executable(qingjian-integration tests/integration.cpp ${QINGJIAN_SOURCES})
  qingjian_target(qingjian-integration)
  target_compile_options(qingjian-integration PRIVATE -UNDEBUG)
  add_test(NAME qingjian-integration COMMAND qingjian-integration)
  set_tests_properties(qingjian-integration PROPERTIES TIMEOUT 10)
  add_executable(qingjian-vkeyboard tests/integration.cpp ${QINGJIAN_SOURCES})
  qingjian_target(qingjian-vkeyboard)
  target_compile_definitions(qingjian-vkeyboard PRIVATE QJ_TEST_VIRTUAL_KEYBOARD=1)
  target_compile_options(qingjian-vkeyboard PRIVATE -UNDEBUG)
  add_test(NAME qingjian-vkeyboard-visible COMMAND qingjian-vkeyboard both qingjian)
  set_tests_properties(qingjian-vkeyboard-visible PROPERTIES TIMEOUT 10)
  foreach(renderer fcitx auto qingjian)
    foreach(mode inline window both)
      add_test(NAME qingjian-display-${renderer}-${mode} COMMAND qingjian-integration ${mode} ${renderer})
      set_tests_properties(qingjian-display-${renderer}-${mode} PROPERTIES TIMEOUT 10)
    endforeach()
  endforeach()
  add_test(NAME qingjian-idle-disconnect COMMAND qingjian-integration both fcitx idle-disconnect)
  set_tests_properties(qingjian-idle-disconnect PROPERTIES TIMEOUT 10)
  add_executable(qingjian-lifecycle tests/lifecycle.cpp ${QINGJIAN_SOURCES})
  qingjian_target(qingjian-lifecycle)
  target_compile_options(qingjian-lifecycle PRIVATE -UNDEBUG)
  add_executable(qingjian-panel tests/panel.cpp ${QINGJIAN_PANEL_SOURCES})
  qingjian_target(qingjian-panel)
  target_compile_options(qingjian-panel PRIVATE -UNDEBUG)
  add_test(NAME qingjian-panel COMMAND qingjian-panel)
  find_program(DBUS_RUN_SESSION dbus-run-session)
  if(DBUS_RUN_SESSION)
    add_executable(qingjian-watchers tests/watchers.cpp ${QINGJIAN_SOURCES})
    qingjian_target(qingjian-watchers)
    target_compile_options(qingjian-watchers PRIVATE -UNDEBUG)
    add_test(NAME qingjian-foreign-callback COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-watchers>)
    set_tests_properties(qingjian-foreign-callback PROPERTIES TIMEOUT 10)
    if(QINGJIAN_RENDER_FFI)
      add_test(NAME qingjian-engine-appearance COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-watchers> appearance)
      set_tests_properties(qingjian-engine-appearance PROPERTIES TIMEOUT 10)
    endif()
    add_executable(qingjian-gnome-origin tests/gnome_origin.cpp)
    target_include_directories(qingjian-gnome-origin PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
    target_link_libraries(qingjian-gnome-origin PRIVATE PkgConfig::FCITX5 Threads::Threads)
    target_compile_options(qingjian-gnome-origin PRIVATE -Wall -Wextra -Werror -UNDEBUG)
    add_test(NAME qingjian-gnome-origin COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-gnome-origin>)
    set_tests_properties(qingjian-gnome-origin PROPERTIES TIMEOUT 10)
    if(QINGJIAN_GNOME_PROBE)
      add_executable(qingjian-gnome-probe-test tests/gnome_probe.cpp src/panel/gnome/probe.cpp)
      qingjian_target(qingjian-gnome-probe-test)
      target_compile_options(qingjian-gnome-probe-test PRIVATE -UNDEBUG)
      add_test(NAME qingjian-gnome-probe COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-gnome-probe-test>)
      add_test(NAME qingjian-gnome-lost-painted COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-gnome-probe-test> lost-painted)
      add_test(NAME qingjian-gnome-invalid-prepare COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-gnome-probe-test> invalid-prepare)
      add_test(NAME qingjian-gnome-geometry COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-gnome-probe-test> geometry)
      add_test(NAME qingjian-gnome-geometry-prepare COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-gnome-probe-test> geometry-prepare)
      add_test(NAME qingjian-gnome-geometry-loop COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-gnome-probe-test> geometry-loop)
      set_tests_properties(qingjian-gnome-geometry qingjian-gnome-geometry-prepare qingjian-gnome-geometry-loop PROPERTIES TIMEOUT 10)
      set_tests_properties(qingjian-gnome-probe PROPERTIES TIMEOUT 10)
      set_tests_properties(qingjian-gnome-lost-painted PROPERTIES TIMEOUT 10)
      set_tests_properties(qingjian-gnome-invalid-prepare PROPERTIES TIMEOUT 10)
    endif()
    add_executable(qingjian-appearance tests/appearance.cpp src/panel/appearance.cpp)
    qingjian_target(qingjian-appearance)
    target_compile_options(qingjian-appearance PRIVATE -UNDEBUG)
    add_test(NAME qingjian-appearance COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-appearance>)
    set_tests_properties(qingjian-appearance PROPERTIES TIMEOUT 10)
    add_executable(qingjian-text-scale tests/text_scale.cpp src/panel/appearance.cpp)
    qingjian_target(qingjian-text-scale)
    target_compile_options(qingjian-text-scale PRIVATE -UNDEBUG)
    add_test(NAME qingjian-text-scale COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:qingjian-text-scale>)
    set_tests_properties(qingjian-text-scale PROPERTIES TIMEOUT 10)
  endif()
  if(QINGJIAN_X11_BACKEND)
    add_executable(qingjian-xcb tests/xcb.cpp ${QINGJIAN_XCB_SOURCES})
    qingjian_target(qingjian-xcb)
    target_compile_options(qingjian-xcb PRIVATE -UNDEBUG)
    add_test(NAME qingjian-xcb COMMAND qingjian-xcb)
    set_tests_properties(qingjian-xcb PROPERTIES TIMEOUT 10 SKIP_RETURN_CODE 77)
    add_executable(qingjian-xcb-health tests/health.cpp ${QINGJIAN_XCB_SOURCES})
    qingjian_target(qingjian-xcb-health)
    target_compile_options(qingjian-xcb-health PRIVATE -UNDEBUG)
    add_test(NAME qingjian-xcb-health COMMAND bash ${CMAKE_CURRENT_SOURCE_DIR}/tests/health.sh $<TARGET_FILE:qingjian-xcb-health>)
    set_tests_properties(qingjian-xcb-health PROPERTIES TIMEOUT 10 SKIP_RETURN_CODE 77)
    if(QINGJIAN_RENDER_FFI)
      # 显式指定 display 才能运行；计时不作为普通无桌面 CTest 的一部分。
      add_executable(qingjian-xcb-timing tests/timing.cpp ${QINGJIAN_XCB_SOURCES})
      qingjian_target(qingjian-xcb-timing)
    endif()
  endif()
  if(QINGJIAN_RENDER_FFI)
    add_executable(qingjian-controller tests/controller.cpp ${QINGJIAN_PANEL_SOURCES})
    qingjian_target(qingjian-controller)
    target_compile_options(qingjian-controller PRIVATE -UNDEBUG)
    add_test(NAME qingjian-controller COMMAND qingjian-controller)
    set_tests_properties(qingjian-controller PROPERTIES TIMEOUT 10)
  endif()
  foreach(scenario focusout-panel focusout-client focusout-client-owned shift-unknown shift-sensitive shift-password deactivate-sensitive deactivate-password deactivate-disabled capability-deactivate)
    add_test(NAME qingjian-${scenario} COMMAND qingjian-lifecycle ${scenario})
    set_tests_properties(qingjian-${scenario} PROPERTIES TIMEOUT 10)
  endforeach()
  add_test(NAME qingjian-uninstall-preserves-data COMMAND bash ${CMAKE_CURRENT_SOURCE_DIR}/../tests/uninstall.sh)
endif()
